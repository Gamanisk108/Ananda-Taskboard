Django>=5.0,<5.3
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
pywebpush>=2.0
openpyxl>=3.1
python-dateutil>=2.9
holidays>=0.50
# Cloudflare R2 (S3-compatible) media uploads
boto3>=1.34
# error monitoring (dormant unless SENTRY_DSN is set)
sentry-sdk[django]>=2.0
# AI task generation (Claude) + document text extraction
anthropic>=0.109
pypdf>=6.0
python-docx>=1.1
# production server / static
gunicorn>=22.0
whitenoise>=6.6
# Postgres driver (production, when DATABASE_URL is set; SQLite locally)
psycopg[binary]>=3.1
# dev / test
pytest>=8.0
pytest-django>=4.8
