# RESUME HERE — Ananda Taskboard conformance + mobile (2026-06-07)

Everything is committed + pushed to GitHub `main` (deployed to Render). Nothing lives only on the local machine. Browser session + local servers are disposable.

## ✅ 2026-06-09 full QA pass (web + mobile, design-practice audit) — DONE
Swept every view (List/Board/Weekly/Monthly, light+dark) + every modal (Task/Subtask/Team/Settings/Export/Import/Bulk/Platform/Approvals/Copy-summary/Projects/Trash/History). Verified: titles=Instrument Sans (never Fraunces), wordmark=Fraunces only, numbers=Red Hat Mono, 5-status colors, **0 native `<select>`**, **0 native color inputs**, **0 overflow/clipping**, create→delete→confirm→trash→purge flow works. **Bugs found + fixed this pass:** (1) DN2 last 2 native selects → `SingleSelect portal` (zero native selects now); (2) native square `<input type=color>` ×4 → circular **`ColorPicker`** w/ recommended palette (hard-rule); (3) `?view=board` deep-link fell back to List (broke Share-view of boards) → fixed; (4) **proj-pills on Trash task rows** (RESUME polish #6) → backend+frontend. Earlier same session: Export/Import ⋯-menu unmount bug, Team-tab `.seg` overflow, D35 spacing token, D36 no-clip rule, mobile sheets/drawer/overflow. Full gate green (tsc/build/73 vitest/backend pytest/eslint-28-baseline).
**Remaining (flagged, non-blocking):** i18n native-speaker pass for `platform.tier`+`ta.shareHint` (View Access, 12 locales) and the machine-translated `noDate` Indic strings; Approvals populated-state (empty verified); deeper mobile native-feel (bottom tab-bar, full-screen task routes — modal-fills-screen works today).

## Where things are
- **Repo:** `Gamanisk108/Ananda-Taskboard`, branch **`main`** (the audit/fix work was merged from `feat/audit-fixes-2026-06-07`).
- **Live build:** https://ananda-taskboard.onrender.com/ (login `admin@ananda.test` / `taskboard123`). Render serves the **committed `frontend/dist`** — so after any `frontend/src` change you MUST `cd frontend && npm run build` and commit `frontend/dist`, then push, or the deploy won't reflect it.
- **Audit docs:** `design/Claude Design/handoff6-6-26eod_COMPLETE/audit/` — `PIXEL-AUDIT-REPORT.md` (full findings + what's fixed), `DECISIONS-NEEDED.md` (DN1–DN13 answers at top), `LOG-UPDATES-for-Claude-Design.md` (D20–D34, hand to Claude Design), `ITEMS-NEEDING-YOU.md`.

## ✅ Done + DEPLOYED + verified live
DB statuses-500 fix · emoji→line-art · Delete-left (D5) · styled confirm dialog (no native confirm) · proj-pills in List+Board+BulkMigrate (C1) · sticky modal footer + "Create task" (DN11/DN8) · status selector on new task (DN9) · inline-title header (DN10) · Links row list + "Links" label (D4) · Help "Admin" chip removed (D17) · account-menu theme dropdown removed (DN6) · Welcome "Skip" removed (D16) · Help & Trash → account menu + lean top bar + What's-New dot (D15) · **auth lotus mark** (P1) · **Unscheduled Tasks emphasized button + rename** (D6) · **mobile List → task cards** (responsive). Tooling: Playwright visual-regression scaffold, `.coderabbit.yaml`, `delete_org` command. ~22 commits, all on `main` + deployed; key surfaces re-audited live (zero console errors).

## ⏳ REMAINING desktop conformance (do next, in this order)
1. **DN2 — native `<select>` → custom popover migration.** ✅ **DONE + verified live (local).** `SingleSelect` built (`components/common.tsx`) and used across the Task popup **and now** ListView filters (Deadline/Recurrence), TeamAdmin (invite/add role+access, member-table role+tier, access subject/scope/level, exclusion type/target — preserves disabled state), Settings (timezone/event-type/end-mode), ExportDialog (format/status/priority/assignee, colored status dots), ImportDialog (format), BulkMigrate (filters + move/status/reassign), AssigneePicker (group), DayTaskList (sort), DeleteWithMove (target), top-bar org switcher + account-menu language. `SingleSelect` enhanced: falsy value (filter "Any" default) shows label but not active styling; numeric width sizes the trigger. Verified zero console errors; `document.querySelectorAll('select').length === 0` on List/Export/Team. **Deferred:** ImportDialog per-row decision selects (inside an `overflow:auto` scroll region → popover would clip; needs portal) and Subtask editor/detail selects (fold into the D11/D12 rebuild).
2. **D6/D7/D13 ✅ DONE + verified live (2026-06-08).** D6 button live; the Unscheduled MODAL is now the standard List table (`UndatedTasks.tsx` → `UnscheduledTable`): Task·Project·Sub-project·Assignees·Status, sortable, filters search+Assignee·Project·Status, **no deadline/recurrence, no "All day", no Time sort**. `cal.noDate`/`cal.noDateTitle` translated in all 12 non-EN locales (were "No date").
3. **Subtask D11/D12 rebuild ✅ DONE + verified live (2026-06-08).** `SubtaskEditor` rows = priority·title·avatar column (◇ groups)·status pill+popover (`StatusPillSelect`)·✕ + header status-count dots + done/total progress bar (To-Do = empty track). `SubtaskDetail` = breadcrumb header `{Parent} #id › {Sub-task} #id.index` (in the modal head, replaces title) + Share + "Sub-task name" + pill+"Change to…" status + Links list + both-or-neither time error + Delete-left/Save (no "Back" button). Added a `links` field to the Subtask model (backend migration 0028).
4. **Polish:** proj-pills on Trash/CopySummary/BulkMigrate rows · Links **label** "Links (one URL per line)"→"Links" (`task.links`, 13 locales) · i18n missing keys (Help/summary-strip/status-pill labels, all 13 locales) · DN1 radii in the design-bundle CSS (`mobile/ananda-mobile.css` 13/10 & `auth/ananda-auth.css` 14/10 → 11/8) · auth screens lotus mark (currently a navy dot).

## MOBILE / RESPONSIVE — STARTED
- ✅ **List → task cards (`.tcard`) on phones (≤700px)** — deployed + verified live at 390 (`useIsNarrow` hook in `common.tsx`; card branch in `ListView.tsx`; `.tcard` CSS in `App.css`). Replaces the side-scrolling table. (Minor polish: card title row alignment.)
- ✅ **Reusable `BottomSheet` primitive** (`common.tsx` + `.sheet-*` CSS; portaled to `<body>`, z-index 130 so it sits above modals, grab handle, Reset/✕, sticky footer, reduced-motion) — verified live 2026-06-08.
- ✅ **List filters → bottom sheet** ; ✅ **viewbar actions → `⋯` overflow menu** ; ✅ **project picker → horizontal-scroll rail** + scrollable summary strip ; ✅ **Monthly calendar fits 7 columns** (was clipping THU–SAT) ; ✅ **assignee picker → bottom sheet** ; ✅ **calendar day-detail → bottom sheet**. All 2026-06-08, verified at 390px.
- ✅ **D35** universal form-spacing token `--gap-form` (killed double margin+gap) ; ✅ **D36** "no hard-clipping" global rule (task title ellipsis + autofocus-only-on-create; clipping audit = 0 offenders on List + task modal).
- ✅ Verified acceptable as-is on mobile: Board (column scroll), Weekly (time-grid scroll), Task modal (fills screen), Team (tabs/tables scroll, forms stack).
- ⏳ Remaining mobile (larger native-feel rebuilds, not breakages): nav drawer / mobile app-bar; full-screen Task-detail/New-task routes (modal currently fills screen — acceptable); compact `.crow` Trash/Approvals; bottom tab-bar nav. Reuse `BottomSheet` where a sheet fits.

## ⏳ MOBILE / RESPONSIVE goal (full)
The live app is a responsive desktop for most surfaces, NOT the full designed mobile UI. Implement the `mobile/` + `app/Ananda Taskboard - Responsive.html` designs: phone List as `.tcard` cards (not a side-scrolling table), nav drawer / ⋯ overflow, bottom sheets (filters/assignee/day-detail), full-screen Task-detail/New-task routes, compact `.crow` Trash/Approvals. See `PIXEL-AUDIT-REPORT.md` §L for the design-vs-live table. **Protocol:** drive the live build at 390/834 via Playwright, compare to the design HTML, implement, re-verify.

## Deploy/ops (Gordon)
- Email: confirm Render env `EMAIL_BACKEND` + `EMAIL_HOST_PASSWORD`(Resend key) + `DEFAULT_FROM_EMAIL` are set (added 2026-06-07). For email-to-anyone, verify a domain in Resend later.
- Test orgs: purge unverified via Render Shell — `printf '\e[?2004l'` then `python backend/manage.py delete_org --purge-unverified --yes --delete-orphan-users` (the command is deployed now).
- CodeRabbit: install the GitHub app on the repo to get PR reviews (config committed).

## Workflow reminders
- After `frontend/src` edits: `npm run build` + commit `frontend/dist` so Render deploys it.
- Verify each change: `cd frontend && npm run build && npx vitest run`; backend `./venv/Scripts/python.exe -m pytest -q`.
- eslint baseline is ~26 errors (pre-existing react-hooks/immutability + 2 react-refresh provider+hook); don't chase those.
