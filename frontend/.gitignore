# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local
# NOTE: dist/ is intentionally committed so the Python-only Render build serves it
# without Node. Rebuild + commit dist after frontend changes.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright (visual regression) — keep __snapshots__ baselines, ignore run artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/
