# @emoji-mart/react@1.1.1 declares a React <=18 peer range but works fine on
# React 19 (the rest of the stack — react-dom, router, i18next, lucide — already
# supports 19). Without this, `npm install` fails on the stale peer range.
legacy-peer-deps=true
