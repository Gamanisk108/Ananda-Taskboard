# Build-time config baked into the committed dist (Render only runs Python, so
# the frontend bundle is built locally — Render env vars can't reach it).
# A Sentry DSN is a PUBLISHABLE client key (it can only ingest events, not read
# anything), so committing it here is safe and standard — hence the .gitignore
# exception for this one file.
VITE_SENTRY_DSN=https://30a3ef787ea9b266ca126c322faf0ca3@o4511543591108608.ingest.us.sentry.io/4511543612276736
